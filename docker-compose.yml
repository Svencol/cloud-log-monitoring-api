services:
  api:
    build: .
    container_name: cloud-log-monitoring-api
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=sqlite:///./logs.db
    volumes:
      - ./logs.db:/app/logs.db